import { For{{pascalCase name}} } from "../app/ports";

export class {{pascalCase name}} implements For{{pascalCase name}} {
	private readonly app: For{{pascalCase name}};

	constructor(app: For{{pascalCase name}}) {
		this.app = app;
	}
}
