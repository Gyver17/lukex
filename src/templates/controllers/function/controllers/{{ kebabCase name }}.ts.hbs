import { Router } from "express";
import { For{{pascalCase name}} } from "../app/ports/{{kebabCase name}}";

export function {{camelCase name}}Http(app: For{{pascalCase name}}Http) {
	const router = Router();

	return router;
}
