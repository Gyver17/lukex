import { Router } from "express";
import { For{{pascalCase name}} } from "../app/ports/for-{{kebabCase name}}";

export function {{camelCase name}}(app: For{{pascalCase name}}) {
	const router = Router();

	return router;
}
