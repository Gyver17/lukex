import { ForShare{{pascalCase name}} } from "../app/ports";

export class Share{{pascalCase name}} implements ForShare{{pascalCase name}} {
	private readonly app: ForShare{{pascalCase name}};

	constructor(app: ForShare{{pascalCase name}}) {
		this.app = app;
	}
}
