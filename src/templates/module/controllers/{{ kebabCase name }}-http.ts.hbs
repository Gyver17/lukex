import { Router } from "express";
import { For{{pascalCase name}}Http } from "../app/ports/{{kebabCase name}}-http";

export function {{camelCase name}}Http(app: For{{pascalCase name}}Http) {
	const router = Router();

	return router;
}
